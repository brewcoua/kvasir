{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "lockFileMaintenance": { "enabled": true },
  "packageRules": [
    {
      "description": "Base and tool images are pinned by digest so a rebuild resolves to the same bytes.",
      "matchManagers": ["dockerfile"],
      "pinDigests": true
    },
    {
      "description": "dspy-ai 2.4.9 is what the fork in src/kvasir/storm was written against, and its API changed substantially afterwards. Raising it is a project, not a dependency bump.",
      "matchPackageNames": ["dspy-ai"],
      "enabled": false
    }
  ]
}
